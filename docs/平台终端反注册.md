反注册流程：
- 平台删除终端信息
- 终端侧等待下一次lastoreAutoCheck定时器触发更新检查，调用平台的version接口
- 如果平台的version的code=416后，就执行反注册流程，调用iup提供的uninstall脚本
- uninstall脚本会卸载iup相关的包，并重新dconfig的override，导致`incremental-update`，`platform-update`等配置更新，
- 之后触发updatesource流程，进行重新检查更新
